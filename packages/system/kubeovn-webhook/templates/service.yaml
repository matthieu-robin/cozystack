apiVersion: v1
kind: Service
metadata:
  name: {{ include "namespace-annotation-webhook.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "namespace-annotation-webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: {{ include "namespace-annotation-webhook.servicePort" . }}
      targetPort: {{ include "namespace-annotation-webhook.port" . }}
      protocol: TCP
      name: https
  selector:
    app.kubernetes.io/name: {{ include "namespace-annotation-webhook.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
