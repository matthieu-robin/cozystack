{{- /*
  Job that materialises a long-lived kubeadm-format bootstrap-token Secret
  inside the tenant kube-system namespace, so that Talos workers can use
  cluster.token from their machineconfig to perform kubelet CSR.

  Why a Job and not a kube-native Secret on the management cluster:
  bootstrap-token-<id> Secrets must live in the tenant apiserver's
  kube-system, which the management cluster can only reach via the
  Kamaji-issued admin kubeconfig stored as <release>-admin-kubeconfig.

  Why not a Helm post-install/post-upgrade hook (the earlier shape):
  post-* hooks execute only after Helm's wait phase resolves. On the
  tenant `kubernetes` release the wait blocks on MachineDeployment Ready,
  which blocks on nodes registering, which blocks on kubelet CSR
  authentication, which needs the bootstrap-token this Job produces —
  a hard deadlock on every fresh install and on every HR reconcile that
  finds MD in a non-Ready state. pre-* hooks are not usable either: they
  fire before non-hook templates apply, so <release>-admin-kubeconfig
  (materialised by Kamaji after KamajiControlPlane is applied) is not
  yet available. The workable shape is a regular resource in the chart:
  it lands together with the KamajiControlPlane and the talos-secrets,
  its Pod stays pending until Kamaji publishes the admin kubeconfig
  Secret (backoffLimit covers the retry window), then runs and completes
  before MachineDeployment can converge. Helm's wait treats the Job as
  another resource to reach terminal state and does not gate it on MD.
*/}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-bootstrap-token-tenant
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubernetes.labels" . | nindent 4 }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-bootstrap-token-tenant
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "kubernetes.labels" . | nindent 4 }}
spec:
  # backoffLimit + restartPolicy: OnFailure gives the Job up to 11 attempts at
  # the tenant apiserver with exponential backoff (capped at 6 min between
  # restarts). Worst case ~15 min, well inside the chart's HR install timeout
  # (release.cozystack.io/helm-install-timeout: 20m on the cozyrds entry) so
  # the Job either succeeds within the HR window or fails the chart upgrade
  # loudly, instead of letting Helm mark the install "successful" while the
  # bootstrap-token Secret never lands in the tenant kube-system.
  backoffLimit: 10
  ttlSecondsAfterFinished: 600
  template:
    spec:
      restartPolicy: OnFailure
      # This Job only talks to the tenant apiserver via the mounted admin
      # kubeconfig (KUBECONFIG below); it never calls the management apiserver,
      # so the management ServiceAccount token is not needed. Drop it.
      automountServiceAccountToken: false
      serviceAccountName: {{ .Release.Name }}-bootstrap-token-tenant
      containers:
        - name: kubectl
          image: "{{ default (.Files.Get "images/kubectl.tag" | trim) .Values.images.kubectl }}"
          env:
            - name: KUBECONFIG
              value: /tenant-admin/super-admin.svc
            - name: TOKEN_ID
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-talos-secrets
                  key: bootstrapTokenId
            - name: TOKEN_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ .Release.Name }}-talos-secrets
                  key: bootstrapTokenSecret
          volumeMounts:
            - name: tenant-admin
              mountPath: /tenant-admin
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            seccompProfile:
              type: RuntimeDefault
          command:
            - sh
            - -ec
            - |
              # The kubectl apply calls below fail with a non-zero exit when
              # the tenant apiserver is unreachable, which restarts the pod
              # under backoffLimit. No internal retry loop needed — Job-level
              # backoff handles the transient unreadiness window after Kamaji
              # bootstraps the konnectivity plane.
              kubectl version --request-timeout=10s

              cat <<EOF | kubectl apply -f -
              apiVersion: v1
              kind: Secret
              metadata:
                name: bootstrap-token-${TOKEN_ID}
                namespace: kube-system
              type: bootstrap.kubernetes.io/token
              stringData:
                token-id: ${TOKEN_ID}
                token-secret: ${TOKEN_SECRET}
                description: "Talos worker bootstrap token, managed by cozystack kubernetes chart"
                usage-bootstrap-authentication: "true"
                usage-bootstrap-signing: "true"
                auth-extra-groups: "system:bootstrappers:kubeadm:default-node-token"
              EOF

              echo "[$(date -u +%H:%M:%S)] bootstrap-token-${TOKEN_ID} ensured"

              # Grant system:nodes the ability to watch the kubernetes
              # Endpoint/EndpointSlice in the default namespace. Talos
              # workers run an internal k8s.EndpointController (apiserver
              # failover discovery) that otherwise spams
              # "endpointslices forbidden" warnings.
              # Note: resourceNames intentionally omitted — Talos's
              # informer issues unbounded list/watch and Kubernetes RBAC
              # only honours resourceNames on list/watch when the client
              # restricts via a field selector, which Talos does not.
              # The Role is namespace-scoped to default, where only the
              # built-in "kubernetes" endpoint lives.
              cat <<'EOF' | kubectl apply -f -
              apiVersion: rbac.authorization.k8s.io/v1
              kind: Role
              metadata:
                name: talos:node-apiserver-endpoint-discovery
                namespace: default
              rules:
                - apiGroups: [""]
                  resources: ["endpoints"]
                  verbs: ["get", "list", "watch"]
                - apiGroups: ["discovery.k8s.io"]
                  resources: ["endpointslices"]
                  verbs: ["get", "list", "watch"]
              ---
              apiVersion: rbac.authorization.k8s.io/v1
              kind: RoleBinding
              metadata:
                name: talos:node-apiserver-endpoint-discovery
                namespace: default
              roleRef:
                apiGroup: rbac.authorization.k8s.io
                kind: Role
                name: talos:node-apiserver-endpoint-discovery
              subjects:
                - kind: Group
                  apiGroup: rbac.authorization.k8s.io
                  name: system:nodes
              EOF

              # Drop the previous ClusterRole/Binding incarnation if it
              # exists from earlier deploys of this chart.
              kubectl delete clusterrole talos:node-apiserver-endpoint-discovery --ignore-not-found
              kubectl delete clusterrolebinding talos:node-apiserver-endpoint-discovery --ignore-not-found

              echo "[$(date -u +%H:%M:%S)] talos:node-apiserver-endpoint-discovery RBAC ensured"
      volumes:
        - name: tenant-admin
          secret:
            secretName: {{ .Release.Name }}-admin-kubeconfig
